Cuddlebys.LotController = Ember.ObjectController.extend({
  // needs: ['auction'],
  // actions: {
  //   delete: function() {
  //     if (confirm('Are you sure you do not want to cuddle with this creature?')) {
  //       alert('OK, we will release it for you.');
  //       // debugger;
  //       var lots = this.get('controllers.auction.lots');
  //       var lot = this.get('model');
  //       lots.removeObject(lot);
  //       lot.destroyRecord();
  //
  //       // auction.get('lots').removeObject(lot).then(function() {
  //       //   lot.destroyRecord();
  //       // });
  //     }
  //   }
  // }
});
